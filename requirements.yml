---
# Ansible Galaxy requirements
# Install required collections and roles

collections:
  - name: community.general
    version: ">=6.0.0"
  - name: kubernetes.core
    version: ">=2.0.0"

roles:
  # Add any required roles here
